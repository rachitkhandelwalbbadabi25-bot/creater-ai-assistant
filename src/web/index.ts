// ════════════════════════════════════════════════════════════════════════════════
// src/web/index.ts — Web dashboard entry point (Placeholder)
// ════════════════════════════════════════════════════════════════════════════════

/**
 * Creater Web Dashboard Entry Point.
 * (Future feature: A React/Next.js dashboard to monitor Creater's state visually).
 */
export const APP_NAME = "Creater AI Assistant";
export const CREATOR = "Creater"; // Fixed "tari" typo to "Creater" as requested

export function initWebDashboard() {
  console.log(`${APP_NAME} web dashboard initialized by ${CREATOR}.`);
}
